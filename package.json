{
  "name": "payrole",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter contracts build && pnpm --filter web build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "serve": "^14.2.6",
    "typescript": "^5.5.0"
  }
}
